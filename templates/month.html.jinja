{% extends "layout/default.html.jinja" %}

{% block content %}
<table>
    {% set crnt_entries = reports[0]["months"][0]["reports"] %}
    {% for entry in crnt_entries -%}
    <tr>
        <td>{{ entry["agency_name"] }}</td>
        <td><a href="{{SITE_PATH}}/{{PATH_GTFS_SCHEDULE}}/{{ entry["path"] }}/report.html">report</a></td>
    </tr>
    {% endfor %}
</table>


{% endblock %}
